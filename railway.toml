[build]
builder = "DOCKERFILE"

[deploy]
startCommand = "/bin/sh -c \"uvicorn web.app:app --host 0.0.0.0 --port $PORT\""
healthcheckPath = "/api/status"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
